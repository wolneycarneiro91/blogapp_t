    <div class="card mt-4">
        <div class="card-body">
            <h1>{{postagem.titulo}}</h1>
            <hr>
            <small>{{postagem.data}}</small><br>
            <h4>{{postagem.conteudo}}</h4>
</div>
</div>